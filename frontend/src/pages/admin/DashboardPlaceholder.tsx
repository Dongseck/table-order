import { PlaceholderLayout } from '../PlaceholderLayout';

export default function DashboardPlaceholder() {
  return (
    <PlaceholderLayout
      unit="Unit 3 (Order + SSE)"
      title="실시간 주문 모니터링"
      description="FR-07: SSE로 테이블별 카드 그리드 실시간 갱신, 상태 변경/삭제."
    />
  );
}
